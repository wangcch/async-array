{
  "name": "@wangcch/async-array",
  "version": "0.1.0",
  "description": "array asynchronous loop",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "build": "rm -rf ./lib/* && tsc -d --project tsconfig.json --outDir ./lib/",
    "test": "rm -rf ./dist/* && tsc -d --project tsconfig.test.json --outDir ./dist/ && ava",
    "prepare": "yarn build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/wangcch/async-array.git"
  },
  "keywords": [
    "array",
    "async",
    "loop",
    "filter"
  ],
  "author": "wangcch <wangcch.cc@gmail.com>",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "ava": "^2.2.0",
    "ts-node": "^8.3.0",
    "typescript": "^3.5.3"
  }
}
